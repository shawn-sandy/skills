{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; d=\"${CLAUDE_PROJECT_DIR:-$PWD}\"; case \"$f\" in \"$d\"/skills/*) o=$(cd \"$d\" && python3 tests/validate_frontmatter.py skills) || { printf '%s\\n' \"$o\" >&2; exit 2; };; esac; }",
            "timeout": 30,
            "statusMessage": "Validating skill frontmatter..."
          }
        ]
      }
    ]
  }
}
